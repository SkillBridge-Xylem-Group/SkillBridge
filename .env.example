# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — required in production
SUPABASE_SERVICE_ROLE_KEY=

# App origin
NEXT_PUBLIC_SITE_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Server-only — required in production (openssl rand -base64 48)
SWAP_CHANNEL_SECRET=

# Optional — comma-separated IPs allowed to access /vault-7q2k and /dashboard/admin
# ADMIN_ALLOWED_IPS=203.0.113.10,198.51.100.5

# Optional
GIPHY_API_KEY=
